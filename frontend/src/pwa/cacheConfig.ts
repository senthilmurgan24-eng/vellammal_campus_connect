export const CACHE_VERSION = 'v1';
export const API_CACHE = `vcc-api-${CACHE_VERSION}`;
export const ASSET_CACHE = `vcc-assets-${CACHE_VERSION}`;
export const IMAGE_CACHE = `vcc-images-${CACHE_VERSION}`;
